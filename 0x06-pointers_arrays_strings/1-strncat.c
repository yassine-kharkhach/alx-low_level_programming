#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src){
        int i = 0;
        int j = 0;

        while (dest[i])
        {
            dest[i] == dest[i];
                i++;
        }
        while (src[j])
        {
                dest[i] = src[j];
                i++;
                j++;
        }
        dest[i] = '\0';
        return(dest);
}i
